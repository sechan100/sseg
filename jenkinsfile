pipeline {
    agent any

    tools {
        jdk 'jdk17'
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build') {
            steps {
               sh '''
                echo 'start bootJar'
                chmod +x gradlew
                ./gradlew clean bootJar
                '''
            }
        }
        stage('Deploy') {
            steps {
                script {
                    docker.build('sseg-app')
                    docker.image('sseg-app').push()
                }
            }
        }
    }
}
